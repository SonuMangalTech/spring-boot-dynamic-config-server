spring:
  application:
    name: DynamicConfigServer

  cloud:
    config:
      server:
        git:
          uri: https://github.com/SonuMangalTech/dynamic-config-repo
          default-label: main

server:
  port: 8888